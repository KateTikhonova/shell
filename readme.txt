Specialized shell processor for Unix (Ubuntu).
I’ve designed my own shell, using parallel processing on C. The program parses the line consisting of common shell’s command and executes it via calls. I used such things as pipes (for pipeline – the pipeline can be any length), fork and exec for this project
